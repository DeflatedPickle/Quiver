plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm' version '1.3.72'

    id 'application'

    id 'com.diffplug.gradle.spotless' version '3.27.0'
}

group 'org.deflatedpickle'
version '1.0.0'

allprojects {
    repositories {
        jcenter()
        mavenCentral()

        maven { url 'https://jitpack.io' }
        maven { url 'https://github.com/eugener/oxbow/raw/master/maven/repository' }
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8"

    compile project(':core')
}

run {
    args = ['indev']

    dependencies {
        compile project(':launcher')
        compile project(':foldertree')
        compile project(':filetable')
        compile project(':filepanel')
    }

    application {
        mainClassName = 'com.deflatedpickle.quiver.launcher.MainKt'
    }
}